@model IEnumerable<DealersTrack.Models.Application>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Dealer.dealerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.customerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dealer.dealerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.customerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.applicationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.applicationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.applicationId })
        </td>
    </tr>
}

</table>
